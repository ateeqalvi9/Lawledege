# TODO
- [x] Replace native <select> elements in `src/Pages/FileComplaint.jsx` (Location, Issue Category, Severity Level) with a custom inline `CustomSelect` component.
- [x] Update `src/Pages/FileComplaint.css` with real custom dropdown styling (colors + hover + focus ring + selected state).
- [x] Verify submission still sends `location`, `category`, `severity` values correctly.
- [x] Run frontend build checks (lint/build) to ensure no syntax errors.


